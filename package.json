{
  "name": "fizz-buzz",
  "module": "index.test.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "(cd nim && nim c -o:nim --verbosity:0 main.nim) & (cd crystal && crystal build src/crystal.cr) & (cd rust && cargo build) & (cd zig && zig build) & (cd onyx && onyx build -o onyx.wasm main.onyx)",
    "format": "deno fmt * --ignore=node_modules"
  }
}
