{
  "name": "fizz-buzz",
  "module": "index.test.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "nim c -o:nim/nim --verbosity:0 nim/main.nim & (cd crystal && crystal build src/crystal.cr) & (cd rust && cargo build) & (cd zig && zig build) & (cd onyx && onyx build -r wasi -o onyx.wasm main.onyx)",
    "format": "deno fmt * --ignore=node_modules"
  }
}
