{
  "name": "fizz-buzz",
  "module": "index.test.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "nim c -o:nim/fizz-buzz --verbosity:0 nim/main.nim & (cd rust && cargo build) & (cd zig && zig build)",
    "format": "deno fmt * --ignore=node_modules"
  }
}
